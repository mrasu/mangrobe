# Repository Guidelines

## Project Structure
- `mangrobe-api-server/`: reference implentation of Mangrobe Protocol using gRPC server.
- `mangrobe-lab/`: example codes.
- `spec/`: protocol definitions (`api.proto`).

## Rule
- Read and follow each project's AGENTS.md when exists
